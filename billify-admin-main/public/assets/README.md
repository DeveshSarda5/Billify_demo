Place the sign-in button Rive asset here as:

- cat-button.riv

The login page now loads it from:

- /assets/cat-button.riv

Expected default state machine inputs:

- hoverLeft
- hoverRight

Optional idle/reset inputs supported dynamically on mouse leave:

- idle
- reset
- toIdle
